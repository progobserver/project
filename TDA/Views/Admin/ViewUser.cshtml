@model IEnumerable<TDA.Models.User>
@addTagHelper*, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<html>
<body>
    <h2>Список пользователей</h2>
    <table class="table">
        <thead>
            <tr><th>Логин</th><th>Роль</th><th>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
                <tr>
                    <td>@user.Username</td>
                    <td>@user.Role.RoleName</td>
                    <td>
                        <a asp-action="EditUser" asp-controller="Admin" asp-route-id="@user.UserId">Изменить</a> |
                        @if(@user.Role.RoleName=="blocked")
                        {
                            <a asp-action="UnlockUser" asp-controller="Admin" asp-route-id="@user.UserId">Разблокировать</a>
                        }
                        else
                        {
                            <a asp-action="BlockUser" asp-controller="Admin" asp-route-id="@user.UserId">Заблокировать</a>

                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
